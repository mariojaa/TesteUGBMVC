@model List<ProdutoViewModel>

<h1 class="text-center display-4">Lista de Produtos em Estoque</h1>

@if (TempData["MensagemSucesso"] != null)
{
    <div class="alert alert-success" role="alert">
        @TempData["MensagemSucesso"]
    </div>
}

@if (TempData["MensagemErro"] != null)
{
    <div class="alert alert-danger" role="alert">
        @TempData["MensagemErro"]
    </div>
}

<div class="container mt-3">
    <input class="form-control" id="buscador" type="search" placeholder="Buscar..." aria-controls="table-produtos">
</div>

<a title="Adicionar Produto" class="btn Adc-Produto" asp-controller="Produto" asp-action="CriarProduto">Cadastrar Produto</a>

@if (Model.Any())
{
    <table id="table-produtos" class="table table-hover">
        <thead class="table-light">
            <tr>
                <th scope="col">Nome do Produto</th>
                <th scope="col">Estoque</th>
                <th scope="col">Estoque Mínimo</th>
                <th scope="col">Setor de Depósito</th>
                <th scope="col">Tipo</th>
                <th scope="col">Código EAN</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.NomeProduto</td>
                    <td>@item.QuantidadeEmEstoque</td>
                    <td>@item.QuantidadeMinimaEmEstoque</td>
                    <td>@item.SetorDeDeposito</td>
                    <td>@item.TipoDoProdutoUnitarioOuPacote</td>
                    <td>@item.CodigoEAN</td>
                    <td class="align-items-baseline">
                        <a title="Editar" class="btnEditar" asp-route-id="@item.Id" asp-controller="Produto" asp-action="EditarProduto">Editar</a>
                        <a href="#" class="btnDeletar" data-value="@item.Id" title="Excluir">Excluir</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
    <!-- Modal -->
    <div class="modal fade" id="exampleModal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-body">
                    <div id="conteudomodal"></div>
                </div>
            </div>
        </div>
    </div>
}
else
{
    <div class="alert alert-info" role="alert">Nenhum produto encontrado.</div>
}



@section Scripts{
    <script type="text/javascript">
        $(document).ready(function () {
            $(".btnDeletar").click(function () {
                var id = $(this).data("value");
                $("#conteudomodal").load("/Produto/DeletarProduto/" + id,
                    function () {
                        $('#exampleModal').modal("show");
                    }
                );
            });

            $('#table-produtos').DataTable({
                "ordering": true,
                "paging": true,
                "searching": true,
                "pageLength": 10,
                "info": false,
                "lengthChange": false,
                "dom": 'lrtip',
                "oLanguage": {
                    "sInfoPostFix": "",
                    "sInfoThousands": ".",
                    "sLoadingRecords": "Carregando...",
                    "sProcessing": "Processando...",
                    "sSearch": "Pesquisa",
                    "sZeroRecords": "Nenhum registro encontrado",
                    "oPaginate": {
                        "sNext": "Próximo",
                        "sPrevious": "Anterior",
                        "sFirst": "Primeiro",
                        "sLast": "Último"
                    },
                    "oAria": {
                        "sSortAscending": ": Ordenar colunas de forma ascendente",
                        "sSortDescending": ": Ordenar colunas de forma descendente"
                    }
                }
            });

            var oTable = $('#table-produtos').DataTable();
            $('#buscador').on("input", function () {
                oTable.search($(this).val()).draw();
            });
        });
    </script>
}
