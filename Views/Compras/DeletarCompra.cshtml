@model ComprasViewModel

<h2>Confirmar Exclusão</h2>

<p>Tem certeza de que deseja excluir a compra com o ID @Model.Id?</p>

<form asp-action="DeletarCompra">
    <input type="hidden" asp-for="Id" />
    <div class="form-group">
        <input type="submit" value="Confirmar Exclusão" class="btn btn-danger" />
        <a asp-action="ListaCompras" class="btn btn-primary">Cancelar</a>
    </div>
</form>
@*@model TesteUGBMVC.Models.ComprasViewModel

<h3 class="display-5 text-center">Deseja realmente excluir a Compra? <b>@Model.NomeProduto</b>?</h3>
<br />
<form method="post" asp-controller="Compras" asp-action="DeletarCompra" asp-route-id="@Model.Id">
    <button type="submit" class="btn btn-danger btn-block">Confirmar</button>
    <a class="btn btn-primary btn-block" asp-controller="Compras" asp-action="ListaCompras">Cancelar</a>
</form>*@
